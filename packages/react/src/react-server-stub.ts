// This file is intentionally empty and used as the "react-server" export condition stub.
// When Next.js App Router's RSC bundler resolves @universal-i18n/react, it picks up this
// file instead of the client entry (which calls createContext at module evaluation time).
// The real client implementation is loaded by the browser/client bundler via the "default"
// / "import" condition.
export {};
